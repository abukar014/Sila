// Update WEB_API_URL to your machine's local IP when testing locally.
// Find it on Mac: System Settings → Wi-Fi → Details → IP Address
// Find it on Windows: run `ipconfig` in terminal → IPv4 Address
// Example: 'http://192.168.1.42:3001'
// For production: replace with the deployed Vercel URL

export const WEB_API_URL = 'http://YOUR_LOCAL_IP:3001'
